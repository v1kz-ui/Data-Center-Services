# UAT Assets

This folder contains the Phase 7 user-acceptance environment assets.

- `phase7_uat_scenarios.json`: scripted workflow pack for admin, operator, and reader validation

The scenario pack is intended to be combined with the controlled reference seed
bundle through `scripts/build_uat_manifest.py`.
