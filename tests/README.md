# Test Layout

- `unit`: deterministic isolated logic tests
- `integration`: database and service integration tests
- `e2e`: full flow and activated-batch behavior tests

The first coding sprint should keep this area green with smoke tests before adding broader coverage.

